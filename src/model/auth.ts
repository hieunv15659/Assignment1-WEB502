class auth {
    name: string;
    email: string;
    sdt: number;
    constructor(name:string,email:string,sdt:number) {
        this.name = name,
        this.email=email,
        this.sdt=sdt
    }
}